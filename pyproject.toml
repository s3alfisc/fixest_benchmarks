[project]
name = "fixest-benchmarks"
version = "0.1.0"
description = "Benchmarks for fixed effects regression packages"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyfixest==0.40.1",
    "linearmodels",
    "statsmodels",
    "formulaic",
    "pandas",
    "pyarrow",
]
